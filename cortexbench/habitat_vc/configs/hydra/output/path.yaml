# @package hydra
run:
  dir: results/${hydra.job.name}/${oc.env:USER}/${WANDB.name}
  subdir: ${hydra.job.num}_${hydra.job.override_dirname}
sweep:
  dir: results/${hydra.job.name}/${oc.env:USER}/${WANDB.name}
  subdir: ${hydra.job.num}_${hydra.job.override_dirname}
